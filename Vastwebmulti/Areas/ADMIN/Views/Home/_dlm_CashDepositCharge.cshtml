@model Vastwebmulti.Areas.ADMIN.Models.CashDepositeAndBank_Info_VM
@using (Ajax.BeginForm("_dlm_CashDepositCharge", "Home", new AjaxOptions { HttpMethod = "POST", OnSuccess = "success2", OnFailure = "failed2", UpdateTargetId = "dlmcash" }))
{
    <div class="cdc-deposit-save-bar">
        <button id="btnsubmit2" type="submit" class="ne-btn-submit cdc-btn-save"><i class="fas fa-save" aria-hidden="true"></i><span>Save Now</span></button>
    </div>
    <div id="tblDealer" class="cdc-deposit-table-host">
        <table class="table table-bordered cdc-deposit-table example-second">
            <thead>
                <tr>
                    <th>User ID</th>
                    <th>Payment&nbsp;Type</th>
                    <th>Charge&nbsp;(%)</th>
                </tr>
            </thead>
            @if (Model.creditchargeDealer != null)
            {
                <tbody id="tbodyshowdealer" class="cdc-deposit-view">
                    @foreach (var item in Model.creditchargeDealer)
                    {
                        <tr>
                            <td>@item.userid</td>
                            <td>@item.ctype</td>
                            <td class="cdc-deposit-charge-val">@item.Charge</td>
                        </tr>
                    }
                </tbody>
                <tbody id="tbodyeditdealer" class="cdc-deposit-edit" style="display:none;">
                    @for (int i = 0; i < Model.creditchargeDealer.Count; i++)
                    {
                        <tr>
                            @Html.HiddenFor(a => a.creditchargeDealer[i].userinfo)
                            <td>@Html.DisplayFor(a => a.creditchargeDealer[i].userid)</td>
                            <td>
                                @Html.HiddenFor(a => a.creditchargeDealer[i].ctype)
                                @Html.DisplayFor(a => a.creditchargeDealer[i].ctype)
                            </td>
                            <td>
                                @Html.EditorFor(a => a.creditchargeDealer[i].Charge, new { htmlAttributes = new { @class = "form-control cdc-cell-input" } })
                                @Html.ValidationMessageFor(a => a.creditchargeDealer[i].Charge)
                            </td>
                        </tr>
                    }
                </tbody>
            }
        </table>
    </div>
}
