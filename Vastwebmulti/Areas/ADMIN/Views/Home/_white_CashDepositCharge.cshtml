@model Vastwebmulti.Areas.ADMIN.Models.CashDepositeAndBank_Info_VM
@using (Ajax.BeginForm("_white_CashDepositCharge", "Home", new AjaxOptions { HttpMethod = "POST", OnSuccess = "success4", OnFailure = "failed4", UpdateTargetId = "whitecash" }))
{
    <div class="cdc-deposit-save-bar">
        <button id="btnsubmit4" type="submit" class="ne-btn-submit cdc-btn-save"><i class="fas fa-save" aria-hidden="true"></i><span>Save Now</span></button>
    </div>
    <div id="tblwhite" class="cdc-deposit-table-host">
        <table class="table table-bordered cdc-deposit-table example-second">
            <thead>
                <tr>
                    <th>User ID</th>
                    <th>Payment&nbsp;Type</th>
                    <th>Charge&nbsp;(%)</th>
                </tr>
            </thead>
            @if (Model.creditchargewhite != null)
            {
                <tbody id="tbodyshowwhite" class="cdc-deposit-view">
                    @foreach (var item in Model.creditchargewhite)
                    {
                        <tr>
                            <td>@item.userid</td>
                            <td>@item.ctype</td>
                            <td class="cdc-deposit-charge-val">@item.Charge</td>
                        </tr>
                    }
                </tbody>
                <tbody id="tbodyeditwhite" class="cdc-deposit-edit" style="display:none;">
                    @for (int i = 0; i < Model.creditchargewhite.Count; i++)
                    {
                        <tr>
                            @Html.HiddenFor(a => a.creditchargewhite[i].userinfo)
                            <td>@Html.DisplayFor(a => a.creditchargewhite[i].userid)</td>
                            <td>
                                @Html.HiddenFor(a => a.creditchargewhite[i].ctype)
                                @Html.DisplayFor(a => a.creditchargewhite[i].ctype)
                            </td>
                            <td>
                                @Html.EditorFor(a => a.creditchargewhite[i].Charge, new { htmlAttributes = new { @class = "form-control cdc-cell-input" } })
                                @Html.ValidationMessageFor(a => a.creditchargewhite[i].Charge)
                            </td>
                        </tr>
                    }
                </tbody>
            }
        </table>
    </div>
}
