@model IEnumerable<Vastwebmulti.Models.Master_Reamin_Cr_report_Result>
@using System.Linq
@{
    ViewBag.Title = "Master_account_detail";
    Layout = "~/Areas/ADMIN/Views/Shared/_Layout.cshtml";

    var rows = Model != null ? Model.ToList() : new List<Vastwebmulti.Models.Master_Reamin_Cr_report_Result>();
    var sumBalance = rows.Sum(aa => aa.remainbal ?? 0m);
    var sumCredit = rows.Sum(aa => aa.cr);
    var rowCount = rows.Count;
    Func<decimal, string> fmt = v => v.ToString("N2");
}

<link href="~/MainFrontDesign/css/admin-account-page-modern.css" rel="stylesheet" />
<link href="~/MainFrontDesign/css/admin-report-page-responsive.css" rel="stylesheet" />

<section class="content fourdesign-section-admin saas-admin-account-page">
    <div class="container-fluid">

        
        <div class="saas-acc-toolbar">
            <ol class="saas-acc-breadcrumb">
                <li><a href="@Url.Action("Dashboard", "Home")"><i class="fas fa-home" aria-hidden="true"></i> Dashboard</a></li>
                <li>M/D Account's Balance</li>
            </ol>
            <div class="saas-acc-toolbar-right">
                <div class="saas-acc-search">
                    <i class="fas fa-search" aria-hidden="true"></i>
                    <input class="form-control" id="myInput" type="text" placeholder="Search firm, name, mobile..." autocomplete="off" />
                </div>
                <div class="saas-acc-export-group">
                    <button type="button" class="saas-acc-btn saas-acc-btn--excel" id="btnExport" onclick="excel()">
                        <i class="fas fa-file-excel" aria-hidden="true"></i> Export
                    </button>
                    <button type="button" class="saas-acc-btn saas-acc-btn--pdf" id="btnPDF" onclick="PDF()">
                        <i class="fas fa-file-pdf" aria-hidden="true"></i> PDF
                    </button>
                </div>
            </div>
        </div>

        
        <div class="saas-acc-table-card" id="masterAccountExport">
            <div class="saas-acc-table-card-head">
                <h3>M/D balance report</h3>
                <span class="saas-acc-row-count"><i class="fas fa-list" aria-hidden="true"></i> @rowCount records</span>
            </div>
            <div class="saas-acc-table-wrap table-responsive">
                <table id="dtBasicExample" class="table saas-acc-table example-second third-design-table">
                    <thead>
                        <tr>
                            <th class="col-text">M/D Firm</th>
                            <th class="col-text">M/D Name</th>
                            <th>Registered No.</th>
                            <th>Account Balance</th>
                            <th>Admin Credit</th>
                        </tr>
                        <tr class="saas-acc-subtotal-row third-tr-color">
                            <th class="col-text" colspan="3">Sub Total</th>
                            <th class="col-balance">₹@fmt(sumBalance)</th>
                            <th class="col-credit">₹@fmt(sumCredit)</th>
                        </tr>
                    </thead>
                    <tbody id="body">
                        @foreach (var item in rows)
                        {
                            <tr>
                                <td class="col-text" data-label="M/D Firm">@item.FarmName</td>
                                <td class="col-text" data-label="M/D Name">@item.SuperstokistName</td>
                                <td data-label="Registered No.">@item.Mobile</td>
                                <td class="col-balance" data-label="Account Balance">@((item.remainbal ?? 0m).ToString("N2"))</td>
                                <td class="col-credit" data-label="Admin Credit">@item.cr.ToString("N2")</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>

    </div>
</section>

<script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/pdfmake/0.1.22/pdfmake.min.js"></script>
<script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/html2canvas/0.4.1/html2canvas.min.js"></script>
<script>
    function excel() {
        var url = '@Url.Action("ExportToExcel_to_Master", "Home")';
        location.href = url;
    }

    function PDF() {
        var el = document.getElementById('masterAccountExport');
        if (!el || typeof html2canvas === 'undefined') return;
        html2canvas(el, {
            onrendered: function (canvas) {
                var data = canvas.toDataURL();
                var docDefinition = {
                    content: [{ image: data, width: 500 }]
                };
                pdfMake.createPdf(docDefinition).download('Master_remain_balance.pdf');
            }
        });
    }

    $(document).ready(function () {
        if ($.fn.DataTable && $('#dtBasicExample').length) {
            $('#dtBasicExample').DataTable({
                orderCellsTop: true,
                paging: false,
                searching: false,
                info: false
            });
        }

        $('#myInput').on('keyup', function () {
            var value = $(this).val().toLowerCase();
            $('#body tr').filter(function () {
                $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1);
            });
        });
    });
</script>
