@model Vastwebmulti.Models.RegisterViewModel
@using Vastwebmulti.Models;
@{
    Layout = null;
}
@{
    VastwebmultiEntities db = new VastwebmultiEntities();
    var ccc = db.tblContects.ToList();
    var companyname = ccc.Count == 0 ? "" : ccc.SingleOrDefault().CompnayName;
    var loginimage = db.tblLoginContents.ToList();
    if (loginimage.Count > 0)
    {
        ViewBag.loginimage = loginimage.FirstOrDefault().Image;
    }
    else
    {
        ViewBag.loginimage = "Login_Images/login22.png";
    }

    var image1 = "";
    var fullpath = "";
    var size = "";
    var repeat = "";
    var height = "";
    var backgroundImage = "";
    var image = db.tblWhiteLabelLoginBackImages.Where(p => p.Role == "ADMIN").ToList();
    if (image.Count > 0)
    {
        //if (image.SingleOrDefault(a=>a.UsedFor=="Repeat" && a.StatusCheck == "Y").otherimage == null)
        if (image.Where(a => a.UsedFor == "Repeat" && a.StatusCheck == "Y").SingleOrDefault() != null)
        {
            //image1 = image.SingleOrDefault().ImageUrl;
            //fullpath = "../FooterImages/" + image1;
            image1 = image.SingleOrDefault(a => a.UsedFor == "Repeat").otherimage;
            var ddd = image1.Replace(@"\", "/");
            fullpath = "../.." + ddd;

        }
        else if (image.Where(a => a.UsedFor == "Single" && a.StatusCheck == "Y").SingleOrDefault() != null)
        {
            image1 = image.SingleOrDefault(a => a.UsedFor == "Single").otherimage;
            var ddd = image1.Replace(@"\", "/");
            fullpath = "../.." + ddd;

            size = "cover";
            repeat = "no-repeat";
            height = "100vh";
        }
        else
        {
            backgroundImage = "linear-gradient(to right , var(--main-bg-lcolor), var(--main-bg-rcolor)) !important";
        }
    }
    else
    {
        image1 = "img32.png";
        fullpath = "../FooterImages/" + image1;
        backgroundImage = "linear-gradient(to right , var(--main-bg-lcolor), var(--main-bg-rcolor)) !important";
    }
    var admindetail = db.Admin_details.ToList();
    var mailid = admindetail.SingleOrDefault().email;
    var mobile = admindetail.SingleOrDefault().mobile;
    var background = fullpath;
    var Firm_Name = admindetail.SingleOrDefault().Companyname;

    var col = db.changecolors.ToList();
    var color = "";
    if (col.Count > 0)
    {
        color = col.SingleOrDefault(p => p.Role == "ADMIN").adminandouterheadercolor;
    }
    else
    {
        color = "Gray";
    }
    var entry = db.RetailerCreationCharges.FirstOrDefault();
    bool isAdminFree = entry?.CreatedByAdminIsFree ?? false;
    bool isSignUpFree = entry?.CreatedBySignUpIsFree ?? false;
    var useGradientBg = !string.IsNullOrEmpty(backgroundImage);
    if (string.IsNullOrEmpty(size)) { size = "cover"; }
    if (string.IsNullOrEmpty(repeat)) { repeat = "no-repeat"; }

    var loginAccent = (color ?? "").Trim();
    var loginAccent2 = loginAccent;
    var accentKey = loginAccent.Replace("#", "").ToLowerInvariant();
    if (string.IsNullOrEmpty(accentKey) ||
        accentKey == "gray" || accentKey == "grey" ||
        accentKey == "black" || accentKey == "white" ||
        accentKey == "808080" || accentKey == "666" || accentKey == "666666" ||
        accentKey == "999" || accentKey == "999999" || accentKey == "ccc" || accentKey == "cccccc")
    {
        loginAccent = "#2563eb";
        loginAccent2 = "#4f46e5";
    }

    var themeTopbar = "#ffffff";
    var themeSidebar = "#0b1220";
    var themeTopbarDark = false;
    try
    {
        var themeRw = new JSONReadWrite();
        var themeRaw = themeRw.Read("admin-layout-theme.json", "~/JsonData/");
        if (!string.IsNullOrWhiteSpace(themeRaw))
        {
            var topbarMatch = System.Text.RegularExpressions.Regex.Match(themeRaw, "\"topbarBg\"\\s*:\\s*\"([^\"]+)\"");
            var sidebarMatch = System.Text.RegularExpressions.Regex.Match(themeRaw, "\"sidebarBg\"\\s*:\\s*\"([^\"]+)\"");
            var topbarDarkMatch = System.Text.RegularExpressions.Regex.Match(themeRaw, "\"topbarDark\"\\s*:\\s*(true|false)", System.Text.RegularExpressions.RegexOptions.IgnoreCase);
            if (topbarMatch.Success && !string.IsNullOrWhiteSpace(topbarMatch.Groups[1].Value))
            {
                themeTopbar = topbarMatch.Groups[1].Value;
            }
            if (sidebarMatch.Success && !string.IsNullOrWhiteSpace(sidebarMatch.Groups[1].Value))
            {
                themeSidebar = sidebarMatch.Groups[1].Value;
            }
            if (topbarDarkMatch.Success)
            {
                themeTopbarDark = string.Equals(topbarDarkMatch.Groups[1].Value, "true", StringComparison.OrdinalIgnoreCase);
            }
        }
    }
    catch { }
}

<!DOCTYPE html>

<html>
<head>
    <title>@companyname</title>
    <!-- Meta tag Keywords -->
    @{
        var favimage = "";
        VastwebmultiEntities db2 = new VastwebmultiEntities();
        var faviconlogo = db2.tblfavicons.Where(p => p.Role == "ADMIN").ToList();
        if (faviconlogo.Count() > 0)
        {
            if (faviconlogo.SingleOrDefault().Role == "ADMIN")
            {
                favimage = faviconlogo.SingleOrDefault().faviconimage;

            }
        }
        else
        {
            <link rel="icon" href="" type="image/x-icon" />
        }
    }
    <link rel="icon" href="@Url.Content("~/" + @favimage)" type="image/x-icon" />
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <meta name="keywords" content="Classic Forms Responsive Widget,Login form widgets, Sign up Web forms , Login signup Responsive web form,Flat Pricing table,Flat Drop downs,Registration Forms,News letter Forms,Elements" />
    <script type="application/x-javascript">
         addEventListener("load", function() { setTimeout(hideURLbar, 0); }, false);
        function hideURLbar(){ window.scrollTo(0,1); } </script>
    <!-- Meta tag Keywords -->
    <!-- css files -->
    <link href="~/NewLoginDesign/css/font-awesome.css" rel="stylesheet" />
    <link href="~/NewLoginDesign/css/style.css" rel="stylesheet" />
    @*<link href="~/CSS/aaaaa.css" rel="stylesheet" />*@
    <link href="~/NewLoginDesign/css/bootstrap-responsive.min.css" rel="stylesheet" />

    <!-- Font-Awesome-Icons-CSS -->
    <!-- //css files -->
    <!-- Web-Fonts -->
    <link href="~/NewLoginDesign/css/owl.carousel.min.css" rel="stylesheet" />
    <link href="//fonts.googleapis.com/css?family=Josefin+Sans:100,100i,300,300i,400,400i,600,600i,700,700i" rel="stylesheet">
    <link href='//fonts.googleapis.com/css?family=Open+Sans:400,600,700' rel='stylesheet' type='text/css'>
    <link href="https://fonts.googleapis.com/css?family=Open+Sans:300,400,600,700|Roboto:100,400,400i,500,500i,700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.1.0/css/bootstrap.min.css">
    <link href="~/MainFrontDesign/css/Custom-style.css" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet" />
    <link href="~/MainFrontDesign/css/public-login-modern.css?v=33" rel="stylesheet" />
    <script src="~/MainFrontDesign/js/public-login-modern.js?v=5"></script>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.0/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.1.0/js/bootstrap.min.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <script src="~/Scripts/owl.carousel.min.js"></script>

    @if (!HttpContext.Current.Request.IsLocal)
    {
        <!-- To Disable Inspect Element -->
        <script src="~/Scripts/Disable_Inspect_Element/Disable_Inspect.js"></script>

    }
    <style>
        .vm-login-page {
            --main-bg-lcolor: @loginAccent;
            --main-bg-rcolor: @loginAccent2;
            --vm-login-accent: @loginAccent;
            --vm-login-page-bg: @themeSidebar;
            --vm-login-visual-bg: @themeSidebar;
            --vm-login-panel-bg: @themeTopbar;
            --vm-login-surface: @themeTopbar;
        }
    </style>
</head>

<body class="body Login_body vm-login-page vm-public-login vm-login-page--theme @(themeTopbarDark ? "vm-login-panel--dark" : "")">
    <div class="vm-login-page__backdrop" aria-hidden="true"></div>
    <div class="main custome-loginpage vm-login-page__wrap vm-public-login__shell">
        <div id="horizontalTab" class="vm-login-page__tabs-root" data-initial-tab="@(ViewBag.signin_signup == "singup" ? "singup" : "singin")">
            <div class="container-fluid">
                <div class="container">
                    <div class="row gg vm-login-card">

                        <div class="col-md-6 border-md gg-item vm-login-card__visual">

                            <div class="vm-login-visual">
                                <div class="vm-login-visual__mesh" aria-hidden="true"></div>
                                <div class="vm-login-visual__overlay"></div>
                                <div class="vm-login-visual__content">
                            @if (ViewBag.Loginsilder != null)
                            {
                                try
                                {
                                    <div id="carousel" class="owl-carousel vm-login-visual__slider">
                                        @foreach (var item in ViewBag.Loginsilder)
                                        {
                                            if (item.Status == "Y")
                                            {
                                                <div class="item">
                                                    <img src="@Url.Content("~/" + item.ImagePath)" alt="Login slide" />
                                                </div>
                                            }
                                            else
                                            {
                                                <div class="item">
                                                    <img src="~/images/Default_Login.jpg" alt="Login slide" />
                                                </div>
                                            }
                                        }
                                    </div>
                                }
                                catch { }
                            }
                            else if (ViewBag.Logo != null)
                            {
                                <div class="vm-login-visual__brand">
                                    <img src="@Url.Content("~/" + ViewBag.Logo)" class="vm-login-visual__logo" alt="@companyname" />
                                    <p class="vm-login-visual__kicker">Secure digital services</p>
                                    <p class="vm-login-visual__tagline">@companyname</p>
                                </div>
                            }
                            else
                            {
                                <div class="vm-login-visual__brand">
                                    <img src="~/Default_Img_Logo/Default_Login_Logo.svg" class="vm-login-visual__logo" alt="@companyname" />
                                    <p class="vm-login-visual__kicker">Secure digital services</p>
                                    <p class="vm-login-visual__tagline">@companyname</p>
                                </div>
                            }
                                </div>
                                <div class="vm-login-visual__foot" style="z-index: 9999;">
                                    <span>24/7 support</span>
                                    <span>Encrypted login</span>
                                </div>
                            </div>
                        </div>

                        <div class="col-md-6 border-md gg-item vm-login-card__panel">
                            <div class="span5 vm-login-panel vm-signin-panel">

                                <div class="resp-tabs-list vm-login-tabs" role="tablist">
                                    <button class="resp-tab-item vm-login-tabs__btn @(ViewBag.signin_signup == "singup" ? "" : "resp-tab-active")" id="singin" value="singin" type="button" role="tab" aria-selected="@(ViewBag.signin_signup == "singup" ? "false" : "true")" onclick="return show('singin');">Sign In</button>
                                    <button class="resp-tab-item vm-login-tabs__btn @(ViewBag.signin_signup == "singup" ? "resp-tab-active" : "")" id="singup" value="singup" type="button" role="tab" aria-selected="@(ViewBag.signin_signup == "singup" ? "true" : "false")" onclick="return show('singup');">Sign Up</button>
                                </div>

                                <div class="resp-tabs-container vm-login-panel__body">
                                    @using (Html.BeginForm("Login", "Account", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form", @id = "UpdateForm" }))
                                    {
                                        @Html.AntiForgeryToken()
                                        @Html.Hidden("latloc")
                                        @Html.Hidden("longloc")
                                        @Html.Hidden("hdaddress")
                                        <div class="tab-1 resp-tab-content vm-login-form-wrap @(ViewBag.signin_signup == "singup" ? "" : "vm-login-panel-active")" id="signinform">
                                            <div class="vm-login-form__head">
                                                <div class="vm-login-form__icon" aria-hidden="true">
                                                    <i class="fa fa-unlock-alt"></i>
                                                </div>
                                                <h1 class="vm-login-form__title">Welcome back</h1>
                                                <p class="vm-login-form__subtitle">Sign in to continue to @(string.IsNullOrWhiteSpace(companyname) ? "your account" : companyname)</p>
                                            </div>
                                            <div class="login-top vm-login-form">

                                                <div class="row">
                                                    <div class="col-md-12">
                                                        <label class="vm-login-label" for="Email">Email / Mobile</label>
                                                        <div class="for-border-effect vm-login-field vm-login-field--icon">
                                                            @Html.ValidationMessageFor(m => m.Email, "", new { @class = "text-danger" })
                                                            <i class="fa fa-envelope-o vm-login-field__glyph" aria-hidden="true"></i>
                                                            @Html.TextBoxFor(m => m.Email, new { @class = "form-control vm-login-input", @placeholder = "Enter Registered Email Id / Mobile No", @required = "", autocomplete = "username" })

                                                            <div class="focus-border"><i></i></div>
                                                        </div>
                                                    </div>
                                                </div>


                                                <div class="row">
                                                    <div class="col-md-12">
                                                        <label class="vm-login-label" for="Password">Password</label>
                                                        <div class="for-border-effect vm-login-field vm-login-field--icon">
                                                            @Html.ValidationMessageFor(m => m.Password, "", new { @class = "text-danger" })
                                                            <i class="fa fa-lock vm-login-field__glyph" aria-hidden="true"></i>
                                                            @Html.PasswordFor(m => m.Password, new { @class = "form-control vm-login-input", @placeholder = "Enter Password", @required = "", autocomplete = "current-password" })
                                                            <button type="button" class="vm-login-eye" id="vmLoginTogglePass" aria-label="Show password"><i class="fa fa-eye"></i></button>
                                                            <div class="focus-border"><i></i></div>
                                                        </div>
                                                    </div>
                                                </div>

                                                <div class="login-bottom vm-login-form__extras">
                                                    <ul>
                                                        <li>
                                                            <label>@Html.CheckBoxFor(m => m.RememberMe)&nbsp;Remember Me&nbsp;?</label>
                                                        </li>
                                                        <li>
                                                            <a href="#" onclick="show(this.id)" id="forgetpass">Forgot password&nbsp;?</a>
                                                        </li>
                                                    </ul>
                                                </div>

                                                <div class="submit vm-login-form__submit">
                                                    <div class="loadingio-spinner-gear-94vo528ab8 first-login hidden">
                                                        <div class="loader"></div>
                                                    </div>

                                                    <input id="login" type="submit" value="Sign In" class="fullbodydycolorbg vm-login-btn vm-login-btn--primary" />
                                                    <span id="login-error" class="locationafterbefore"></span>
                                                    <span id="login-error1"></span>
                                                    <span id="login-error2" class="locationafterbefore hidden"></span>
                                                </div>

                                                @*<input id="login" type="submit" value="LOGIN" class="fullbodydycolorbg">*@

                                            </div>
                                        </div>


                                    }


                                    @if (isAdminFree || isSignUpFree)   // dono OFF hone par hi form show nhi hoga
                                    {

                                        using (Html.BeginForm("Insert_retailer", "Account", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form", @id = "frmsignup" }))
                                        {
                                            @Html.AntiForgeryToken()

                                            @Html.Hidden("Email_Id")
                                            @Html.Hidden("Mobile_Id")



                                            <div class="tab-1 resp-tab-content vm-login-form-wrap @(ViewBag.signin_signup == "singup" ? "vm-login-panel-active" : "")" id="signupform">
                                                @* <div class="vm-login-form__head">
                                                    <div class="vm-login-form__icon" aria-hidden="true">
                                                        <i class="fa fa-user-plus"></i>
                                                    </div>
                                                    <h1 class="vm-login-form__title">Create account</h1>
                                                    <p class="vm-login-form__subtitle">Register with @companyname in a few steps</p>
                                                </div> *@
                                                <div class="login-top sign-top vm-login-form">

                                                    <div class="row">
                                                        <div class="col-md-12 vm-login-captcha-wrap">
                                                            <div class="vm-login-captcha-alert">
                                                                <div class="vm-login-captcha-title">
                                                                    🔑 Please verify the Captcha first.
                                                                </div>
                                                                <div class="vm-login-captcha-body">
                                                                    <div class="vm-login-captcha-box">
                                                                        <span id="captcha-text" class="fw-bold text-dark">
                                                                            @ViewBag.CaptchaCode
                                                                        </span>
                                                                        <button type="button" id="refreshCaptcha" class="vm-login-captcha-refresh" aria-label="Refresh Captcha">
                                                                            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 30 30">
                                                                                <path d="M 15 3 C 12.031398 3 9.3028202 4.0834384 7.2070312 5.875 A 1.0001 1.0001 0 1 0 8.5058594 7.3945312 C 10.25407 5.9000929 12.516602 5 15 5 C 20.19656 5 24.450989 8.9379267 24.951172 14 L 22 14 L 26 20 L 30 14 L 26.949219 14 C 26.437925 7.8516588 21.277839 3 15 3 z M 4 10 L 0 16 L 3.0507812 16 C 3.562075 22.148341 8.7221607 27 15 27 C 17.968602 27 20.69718 25.916562 22.792969 24.125 A 1.0001 1.0001 0 1 0 21.494141 22.605469 C 19.74593 24.099907 17.483398 25 15 25 C 9.80344 25 5.5490109 21.062074 5.0488281 16 L 8 16 L 4 10 z"></path>
                                                                            </svg>
                                                                        </button>
                                                                    </div>
                                                                    <div class="vm-login-captcha-action">
                                                                        <input type="text" class="form-control vm-login-captcha-input" id="CaptchaInput" name="CaptchaInput" placeholder="Enter Captcha" required>
                                                                        <button type="button" id="verifyCaptchaBtn" class="vm-login-captcha-verify">Verify</button>
                                                                        <div class="spinner-border text-primary ms-2 d-none vm-login-captcha-spinner" id="captcha_spinner" style="width:20px;height:20px;border-width:2px;"></div>
                                                                        <img id="captcha_success_img" src="~/images/check_img_input.jpg" class="d-none ms-2 vm-login-captcha-success" alt="Verified" style="width:24px;height:24px;" />
                                                                    </div>
                                                                </div>
                                                                <small id="captcha_msg" class="form-text mt-1 text-center w-100"></small>
                                                            </div>

                                                            <div class="for-border-effect">
                                                                @Html.ValidationMessageFor(m => m.Mobile, "", new { @class = "text-danger" })
                                                                @Html.TextBoxFor(m => m.Mobile, new { @class = "form-control phone", onkeypress = "return isNumber(event)", maxlength = "10", @placeholder = "Enter Mobile No", @required = "", @field = "mobile", @id = "Mobile", disabled = "disabled" })


                                                                <small id="mobile_err"></small>
                                                                <div class="spinner-border hidden vm-login-field-spinner" id="Mobileno_spinner"></div>
                                                                <img class="hidden vm-login-field-success" id="success_mobile_img" src="~/images/check_img_input.jpg" alt="Verified" />
                                                                <div class="otp_m">
                                                                    <div class="form_otp" id="otp_continer_inputs">
                                                                        <div class="otp_block">
                                                                            <p class="opt_pra_1"> OTP sent successfully to <br />entered mobile number</p>

                                                                        </div>
                                                                        <div class="otp_set">
                                                                            <input id="codeBox1" type="number" maxlength="1" onkeyup="onKeyUpEvent(1, event)" onfocus="onFocusEvent(1)" />
                                                                            <input id="codeBox2" type="number" maxlength="1" onkeyup="onKeyUpEvent(2, event)" onfocus="onFocusEvent(2)" />
                                                                            <input id="codeBox3" type="number" maxlength="1" onkeyup="onKeyUpEvent(3, event)" onfocus="onFocusEvent(3)" />
                                                                            <input id="codeBox4" type="number" maxlength="1" onkeyup="onKeyUpEvent(4, event)" onfocus="onFocusEvent(4)" />
                                                                        </div>
                                                                    </div>
                                                                </div>
                                                                <div class="focus-border"><i></i></div>
                                                            </div>
                                                        </div>
                                                    </div>
                                                    <div class="row">
                                                        <div class="col-md-12">
                                                            <div class="for-border-effect">
                                                                @Html.ValidationMessageFor(m => m.Email, "", new { @class = "text-danger" })
                                                                @Html.TextBoxFor(m => m.Email, new { @class = "form-control email", @type = "email", @placeholder = "Enter Email-ID", @required = "", @field = "email" })
                                                                <small id="email_err"></small>
                                                                <input type="button" name="button" value="Send OTP" id="btnsendOTP_Email" class="vm-login-send-otp-btn">
                                                                <div class="spinner-border hidden vm-login-field-spinner" id="Email_spinner"></div>
                                                                <img class="hidden vm-login-field-success" id="success_email_img" src="~/images/check_img_input.jpg" alt="Verified" />
                                                                <div class="form_otp vm-login-email-otp" id="otp_continer_inputs_email">
                                                                    <div class="otp_block_1">
                                                                        <p class="opt_pra"> OTP sent successfully to <br />entered Email Id</p>
                                                                    </div>
                                                                    <div class="otp_set_1">
                                                                        <input id="codeBox5" type="number" maxlength="1" onkeyup="onKeyUpEvent_Email(5, event)" onfocus="onFocusEvent(5)" />
                                                                        <input id="codeBox6" type="number" maxlength="1" onkeyup="onKeyUpEvent_Email(6, event)" onfocus="onFocusEvent(6)" />
                                                                        <input id="codeBox7" type="number" maxlength="1" onkeyup="onKeyUpEvent_Email(7, event)" onfocus="onFocusEvent(7)" />
                                                                        <input id="codeBox8" type="number" maxlength="1" onkeyup="onKeyUpEvent_Email(8, event)" onfocus="onFocusEvent(8)" />
                                                                    </div>
                                                                </div>

                                                                <div class="focus-border"><i></i></div>
                                                            </div>
                                                        </div>
                                                    </div>

                                                    <div class="row">
                                                        <div class="col-md-12">
                                                            <div class="for-border-effect">
                                                                @Html.ValidationMessageFor(m => m.Name, "", new { @class = "text-danger" })
                                                                @Html.TextBoxFor(m => m.Name, new { @class = "form-control name", @placeholder = "Enter Customer Name", @required = "" })
                                                                <div class="focus-border"><i></i></div>
                                                            </div>
                                                        </div>
                                                    </div>




                                                    @*    ------------kyc Dropdown-------------------*@
                                                    <div class="row">
                                                        <div class="col-md-12">
                                                            <div class="for-border-effect">

                                                                <select id="KYCType" name="KYCType" class="form-control" required>

                                                                    <option value="">Select KYC Type</option>
                                                                    
                                                                    @foreach (var item in ViewBag.KYCSetting)
                                                                    {
                                                                        <option value="@item.KYCType">
                                                                            @item.DisplayName
                                                                        </option>
                                                                    }

                                                                </select>

                                                                <div class="focus-border"><i></i></div>

                                                            </div>
                                                        </div>
                                                    </div>
                                                    

                                                    <div class="row">
                                                        <div class="col-md-12">
                                                            <div class="for-border-effect">
                                                                @Html.TextBoxFor(m => m.Companyname, new { @class = "form-control phone", maxlength = "40", @placeholder = "Enter Business Name(Optional)" })
                                                                <div class="focus-border"><i></i></div>
                                                            </div>
                                                        </div>
                                                    </div>

                                                    <div class="row">
                                                        <div class="col-md-6">
                                                            <div class="for-border-effect">
                                                                @Html.ValidationMessageFor(m => m.Password, "", new { @class = "text-danger" })
                                                                @Html.TextBoxFor(m => m.Password, new { @class = "form-control phone", maxlength = "10", @placeholder = "Enter Password", @required = "" })
                                                                <div class="focus-border"><i></i></div>
                                                            </div>
                                                        </div>
                                                        <div class="col-md-6">
                                                            <div class="for-border-effect">
                                                                @Html.ValidationMessageFor(m => m.Pin, "", new { @class = "text-danger " })
                                                                @Html.TextBoxFor(m => m.Pin, new { @class = "form-control phone", maxlength = "6", minlength = "4", @placeholder = "Enter Pin", @required = "", onkeypress = "return isNumber(event)" })
                                                                <div class="focus-border"><i></i></div>
                                                            </div>
                                                        </div>
                                                    </div>

                                                    <div class="row">
                                                        <div class="col-md-6">
                                                            <div class="for-border-effect custom-district">
                                                                @Html.ValidationMessageFor(m => m.state, "", new { @class = "text-danger" })
                                                                @Html.DropDownListFor(model => model.state, (IEnumerable<SelectListItem>)ViewBag.Sate, "Select State", new { @class = "form-control", @onchange = "FillDistict()", @required = "" })
                                                                <div class="focus-border"><i></i></div>
                                                            </div>

                                                        </div>
                                                        <div class="col-md-6">
                                                            <div class="for-border-effect custom-district">
                                                                @Html.ValidationMessageFor(m => m.distict, "", new { @class = "text-danger" })
                                                                @Html.DropDownListFor(model => model.distict, new SelectList("Value", "Text"), "Select District", new { @class = "form-control", @required = "" })
                                                                <div class="focus-border"><i></i></div>
                                                            </div>


                                                        </div>
                                                    </div>



                                                    <div class="login-bottom">
                                                        <div class="row">
                                                            <div class="col-md-6">
                                                                <ul>
                                                                    <li>
                                                                        <label>@Html.CheckBoxFor(m => m.RememberMe)&nbsp;Remember Me&nbsp;?</label>
                                                                    </li>

                                                                </ul>
                                                            </div>
                                                            <div class="col-md-6 vm-login-referral-col">
                                                                @*<input type="checkbox" id="referrral" onclick="myFunction()" style="float:left;"><span id="referrral1" style="float: left;font-size: 15px;margin-top: -5px;margin-left:3px;color:#999;">Have a referral code</span>*@
                                                                <div class="for-border-effect vm-login-referral-field">
                                                                    @Html.ValidationMessageFor(m => m.referralcode, "", new { @class = "text-danger" })
                                                                    @if (TempData["referralerror"] != null)
                                                                    {
                                                                        <span class="text-danger">@TempData["referralerror"]</span>
                                                                    }
                                                                    @Html.TextBoxFor(m => m.referralcode, new { @class = "form-control vm-login-referral-input", @placeholder = "Referral Code", @maxlength = "10" })
                                                                    <div class="focus-border"><i></i></div>
                                                                </div>
                                                            </div>
                                                        </div>
                                                    </div>





                                                    <div class="submit vm-login-form__submit">
                                                        <div class="loadingio-spinner-gear-94vo528ab8 second-login hidden">
                                                            <div class="loader"></div>
                                                        </div>
                                                        <input id="SignUp" type="submit" disabled="disabled" value="Create Account" class="fullbodydycolorbg vm-login-btn vm-login-btn--primary" />
                                                    </div>

                                                    @* <input type="submit" id="SignUp" data-loading-text="<i class='fa fa-spinner fa-spin'></i>" value="Sign Up Now" class="fullbodydycolorbg">*@
                                                    @*<button type="submit" onclick="sendLove(this);"   class="fullbodydycolorbg SignUp">Sign Up Now</button>*@
                                                </div>
                                            </div>
                                        }

                                    }
                                    else
                                    {
                                        <div class="tab-1 resp-tab-content vm-login-form-wrap @(ViewBag.signin_signup == "singup" ? "vm-login-panel-active" : "")" id="signupform">
                                            <div class="alert alert-info text-center p-3 rounded">
                                                <strong>ℹ️ Registration is currently free.</strong><br />
                                                No sign-up form is required at this time.
                                            </div>
                                        </div>
                                    }



                                    <div class="tab-1 resp-tab-content hidden vm-login-form-wrap" id="forget">
                                        <div class="vm-login-form__head">
                                            <h1 class="vm-login-form__title vm-login-forget-title">Reset password</h1>
                                            <p class="vm-login-form__subtitle">Enter your registered email or mobile number</p>
                                        </div>
                                        @using (Html.BeginForm("ForgotPasswordAdmin", "Account", FormMethod.Post, new { @class = "form-horizontal", role = "form", @id = "ForgtFrm" }))
                                        {
                                            @Html.AntiForgeryToken()
                                            <div class="login-top sign-top vm-login-form">
                                                <div class="row">
                                                    <div class="col-md-12">

                                                        @Html.ValidationMessageFor(m => m.Email, "", new { @class = "text-danger" })
                                                        @Html.TextBoxFor(m => m.Email, new { @class = "form-control", @placeholder = "Enter Email ID/Mobile No", required = "", id = "yes" })

                                                        @*@Html.ActionLink("Send OTP", "Passcode", "Home", new { ids = Model.Email })*@
                                                        @*<input type="button" onclick="otpsend()" value="Send OTP" />*@
                                                        @{
                                                            var asd = Convert.ToString(Session["chk1"]);

                                                            if (asd != null && asd != "0" && asd != "")
                                                            {

                                                                <a href="#" onclick="otpsend('12')" id="forgetpass" class="hide_otp1 vm-login-forget-otp-link">Send OTP</a>

                                                                <div class="hide_otp">
                                                                    <div class="row clearfix ">
                                                                        <div class="col-md-4">
                                                                            <label class="required vm-login-forget-otp-label">Enter&nbsp;OTP</label>
                                                                        </div>

                                                                        <div class="col-md-8">


                                                                            @*<input type="number" id="OTp" name="OTp" placeholder="Enter OTP" required="required" maxlength="4" style="height: 31px; width: 100%; border: 2px solid #dad9df; outline: none; text-align: center; font-size: 1.5em; border-radius: -1.7em; background-color: #ffffff; outline: none; margin-top: 6px; " />*@
                                                                            <div class="inputfield vm-login-forget-otp-field">


                                                                                <div class="otp_set2">

                                                                                    <input class="tested" name="oTP" type="number" maxlength="1" onkeyup="onKeyUpEvent(1, event)" onfocus="onFocusEvent(1)" required>
                                                                                    <input class="tested" name="oTP1" type="number" maxlength="1" onkeyup="onKeyUpEvent(2, event)" onfocus="onFocusEvent(2)" required>
                                                                                    <input class="tested" name="oTP2" type="number" maxlength="1" onkeyup="onKeyUpEvent(3, event)" onfocus="onFocusEvent(3)" required>
                                                                                    <input class="tested" name="oTP3" type="number" maxlength="1" onkeyup="onKeyUpEvent(4, event)" onfocus="onFocusEvent(4)" required>



                                                                                </div>

                                                                            </div>




                                                                        </div>
                                                                    </div>
                                                                </div>
                                                            }
                                                        }
                                                    </div>
                                                </div>

                                                <div class="login-bottom vm-login-form__extras">

                                                    <div class="clear"></div>
                                                </div>

                                                <div class="submit vm-login-form__submit">
                                                    <div class="loadingio-spinner-gear-94vo528ab8 third-login hidden">
                                                        <div class="loader"></div>
                                                    </div>
                                                    <input id="btnforget" type="submit" value="Reset Password" class="vm-login-btn vm-login-btn--primary fullbodydycolorbg" />
                                                </div>

                                                
                                            </div>
                                        }
                                    </div>
                                </div>

                            </div>
                        </div>

                    </div><!--.row-->
                </div><!--.container-->
            </div>

        </div><!--#horizontalTab-->



        <div class="footer vm-login-page__footer">
            <center> <p> &copy; 2019 - @DateTime.Now.Year All Rights Reserved | @Firm_Name </p></center>
        </div>

    </div><!--.main-->




<script>

    $("#KYCType").change(function () {

        var type = $(this).val();

        $("#ZeroDiv").hide();
        $("#DigitalDiv").hide();
        $("#PhysicalDiv").hide();
        $("#DigitalPhysicalDiv").hide();

        if (type == "ZERO") {

            $("#ZeroDiv").show();

        }
        else if (type == "DIGITAL") {

            $("#DigitalDiv").show();

        }
        else if (type == "PHYSICAL") {

            $("#PhysicalDiv").show();

        }
        else if (type == "DIGITAL_PHYSICAL") {

            $("#DigitalPhysicalDiv").show();

        }

    });

    $(document).ready(function () {

        $("#KYCType").trigger("change");

    });

</script>



    @*<script src="~/AdminDesign/plugins/bootstrap-select/js/bootstrap-select.js"></script>*@
    @*<script src="~/NewLoginDesign/js/jquery-2.1.4.min.js"></script>*@
    <script src="~/Scripts/jquery.validate.js"></script>
    <script src="~/Scripts/jquery.validate.min.js"></script>
    @*<script src="~/Scripts/jquery.validate.unobtrusive.js"></script>*@
    <script src="~/NewLoginDesign/js/easyResponsiveTabs.js"></script>
    <script src="~/Swalalert/sweetalert-dev.js"></script>
    <link href="~/Swalalert/sweetalert.css" rel="stylesheet" />



    <script>
        function toggleSignupForm(adminFree, signupFree) {
            if (adminFree || signupFree) {
                $("#frmsignup").show(); // koi bhi ek ON hai to form dikhao
            } else {
                $("#frmsignup").hide(); // dono OFF hai to form hide
            }
        }

    </script>



    <script>
@* otp sect valu *@
        var container = document.getElementsByClassName("otp_set2")[0];
        container.onkeyup = function (e) {
            var target = e.srcElement || e.target;
            var maxLength = parseInt(target.attributes["maxlength"].value, 10);
            var myLength = target.value.length;
            if (myLength >= maxLength) {
                var next = target;
                while (next = next.nextElementSibling) {
                    if (next == null)
                        break;
                    if (next.tagName.toLowerCase() === "input") {
                        next.focus();
                        break;
                    }
                }
            }
            // Move to previous field if empty (user pressed backspace)
            else if (myLength === 0) {
                var previous = target;
                while (previous = previous.previousElementSibling) {
                    if (previous == null)
                        break;
                    if (previous.tagName.toLowerCase() === "input") {
                        previous.focus();
                        break;
                    }
                }
            }
        }

    @* otp sect valu end *@

        @*$(document).ready(function () {
            $(".hide_otp1").click(function () {
                $(".hide_otp").show();
            });
        });
*@



        $("#UpdateForm").submit(function (e) {

            if ($("#UpdateForm").valid()) {
                // ajax call
                if (document.readyState !== "complete") {
                    $('#login').show();
                    $('.loadingio-spinner-gear-94vo528ab8.first-login').hide()
                } else {
                    $('#login').hide();
                    $('.loadingio-spinner-gear-94vo528ab8.first-login').show()

                }
            }
            else {
                e.preventDefault();
                return false;
            }

            //$('#btnnewlogin').hide();
            //$('.loadingio-spinner-gear-94vo528ab8.first-login').show()
        })
        $("#frmsignup").submit(function (e) {

            if ($("#frmsignup").valid()) {
                // ajax call
                if (document.readyState !== "complete") {
                    $('#SignUp').show();
                    $('.loadingio-spinner-gear-94vo528ab8.second-login').hide()
                } else {
                    $('#SignUp').hide();
                    $('.loadingio-spinner-gear-94vo528ab8.second-login').show()

                }
            }
            else {
                e.preventDefault();
                return false;
            }


            //  $('#btnsignup').hide();
            //  $('.loadingio-spinner-gear-94vo528ab8').show()

        })


        $("#ForgtFrm").submit(function (e) {

            //  $('#btnsignup').hide();
            //  $('.loadingio-spinner-gear-94vo528ab8').show()

            if ($("#ForgtFrm").valid()) {
                // ajax call
                if (document.readyState !== "complete") {
                    $('#btnforget').show();
                    $('.loadingio-spinner-gear-94vo528ab8.third-login').hide()
                } else {
                    $('#btnforget').hide();
                    $('.loadingio-spinner-gear-94vo528ab8.third-login').show()

                }
            }
            else {
                e.preventDefault();
                return false;
            }



        })
    </script>
    <script>
        document.onreadystatechange = function () {
            if (document.readyState !== "complete") {
                document.querySelector(
                    "#login").style.visibility = "visible";
                document.querySelector(
                    ".loadingio-spinner-gear-94vo528ab8.first-login").style.display = "none";
                $('#btnforget').show();
                $('.loadingio-spinner-gear-94vo528ab8.third-login').hide()
            } else {
                document.querySelector(
                    "#login").style.display = "block";
                document.querySelector(
                    ".loadingio-spinner-gear-94vo528ab8.first-login").style.display = "none";


            }
        };
    </script>



    @*<script>
            function sendLove(this1) {
                //alert('asdasd');
                this1.disabled = true;
                this1.innerHTML = '<i class="fa fa-spinner fa-spin"></i>';
            }


            $("#myform").submit(function (e) {
                $('.SignUp').show('<i class="fa fa-spinner fa-spin"></i>');
            });
        </script>*@

    @* Tab + forgot-password panel switching handled in public-login-modern.js *@
    <script>
        $('#viewmore, #viewless').on('click', function () {
            if (this.id == 'viewmore') {
                $('#allcard').show();
                $('#silderid').hide();
                $('#viewless').show();
                $('#viewmore').hide();
            }
            else {
                $('#allcard').hide();
                $('#silderid').show();
                $('#viewless').hide();
                $('#viewmore').show();
            }
        });
    </script>



</body>
</html>


<script>

    function otpsend() {

        var id = $('#yes').val();
        $.ajax({
            url:   '@Url.Action("Passcode", "Home")',

            type: 'GET',
            data: { ids: id },

        dataType: "JSON",


        })
    }
</script>
<script>

    async function reverseGeocode(latitude, longitude) {
        //let apikey = "60baa3a9b6154ef3a3ab635c8bd06767";
        try {
            var url = '@Url.Action("fetchlatlong", "Home")';
            url = url + '?lat=' + latitude + '&lon=' + longitude + '';
            const chk = await fetch(url).then((response) => response.json());
            let apikey = chk.key;
            if (chk.sts == true) {
                $('#hdaddress').val(chk.resp);
            } else {
                const resp = await fetch('https://api.opencagedata.com/geocode/v1/json'
                    + '?'
                    + 'key=' + apikey
                    + '&q=' + encodeURIComponent(latitude + ',' + longitude)
                    + '&pretty=1'
                    + '&no_annotations=1')
                    .then((response) => response.json());

                if (resp.status.code == 200) {
                    $('#hdaddress').val(JSON.stringify(resp.results[0].components));
                }
                else {
                    $('#hdaddress').val("Location Not Fetch");
                }
            }
        }
        catch {
            $('#hdaddress').val("Location Not Fetch");
        }
    }

    function success(data) {
        let latitude = data.coords.latitude;
        let longitude = data.coords.longitude;
        $('#latloc').val(latitude);
        $('#longloc').val(longitude);

        reverseGeocode(latitude, longitude);
    }

    if (navigator.geolocation) {
        window.navigator.geolocation
            .getCurrentPosition(success, showError);
    }

    function showError(error) {
        $('#lblerrormsg').text('')
        switch (error.code) {
            case error.PERMISSION_DENIED:
                // alert("User denied the request for Geolocation.")
                $('#login-error2').html("<i class='material-icons'>add_location</i> User denied the request for Geolocation.");
                break;
            case error.POSITION_UNAVAILABLE:
                //  alert("Location information is unavailable.")
                $('#login-error2').html("<i class='material-icons'>add_location</i> Location information is unavailable.")
                break;
            case error.TIMEOUT:
                // alert("The request to get user location timed out.")
                $('#login-error2').html("<i class='material-icons'>add_location</i> The request to get user location timed out.")
                break;
            case error.UNKNOWN_ERROR:
                $('#login-error2').text("An unknown error occurred.")
                // alert("An unknown error occurred.")
                break;
        }
    }
  

    $('#UpdateForm').submit(function (e) {
        if (location.protocol == 'https:' || location.href.includes("http://localhost/")) {
            if ($('#latloc').val() == "" || $('#longloc').val() == "" || $('#hdaddress').val() == "") {
                e.preventDefault();
                if ($('#login-error2').text() == "") {
                    $('#login-error2').html("<i class='material-icons'>add_location</i> Please allow the Location access");
                }
                $('#login-error').html($('#login-error2').html())
                $('.loadingio-spinner-gear-94vo528ab8.first-login').hide();

    //            $('#login').show();

                navigator.permissions && navigator.permissions.query({ name: 'geolocation' })
                    .then(function (PermissionStatus) {
                        if (PermissionStatus.state == 'granted') {
                        //    alert("allowed")
                             return true;
                            //allowed
                        } else if (PermissionStatus.state == 'prompt') {
                           // alert("prompt - not yet grated or denied")
                            return false;
                            // prompt - not yet grated or denied
                        } else {
                           // alert("denied");
                            return false;
                            //denied
                        }
                    })


            }
        }
    })


</script>


<script>
    $(function () {
          var message = '@ViewBag.messagechkk'
       if (message != null && message != "") {
           $('#login-error').text(message)
        }
        var status = '@TempData["defaultstatus"]';
        if (status != null && status != "") {
            $('#login-error').text(status)
        }

        var reg = '@ViewData["success"]';
        if (reg != null && reg != "") {
            var vv = reg.replace(".", "\n");
            $('#login-error1').html('&#9989;'+reg)
        }

        var slaberror = '@ViewData["slaberror"]';
        if (slaberror != null && slaberror != "") {
            $('#login-error').text(slaberror)
        }
        var email = '@ViewData["email"]'
        if (email != null && email != "") {
            $('#login-error').text(email)
         }
        var mobile = '@ViewData["mobilecon"]'
        if (mobile != null && mobile != "") {
            $('#login-error').text(mobile)
        }
        var message = '@ViewData["wrongpass"]'
        if (message != "") {
            if (message == "Failure") {
                $('#login-error').text('User ID Or Password Wrong')
            }
        }
        var userMsg = '@ViewData["userblock"]'
        if (userMsg != "") {
            $('#login-error').text(userMsg)
        }
        var existuser = '@ViewData["existuser"]'
        if (existuser != "" && existuser != null) {
            $('#login-error').text(existuser)
        }

        var ch2 = '@ViewData["confrim"]';
        if (ch2 != "" && ch2 != null) {
            if (ch2 == "Confrimed") {
                $('#login-error').text('Your Email verification is pending. Please verify the Email first')

            }
        }
        var block = '@ViewData["msg"]';
        if (block == "websiteblock") {
            $('#btnsleep').click();
            $("#sleep-modal").modal('show');
        }
        $(".sweet-alert").css('background', '@PageData["globalOuter"]');
    });
</script>
<script>
    function isNumber(evt) {
        evt = (evt) ? evt : window.event;
        var charCode = (evt.which) ? evt.which : evt.keyCode;
        if (charCode > 31 && (charCode < 48 || charCode > 57)) {
            return false;
        }
        return true;
    }
</script>
<!--fill district javascript-->
<script>
    function FillDistict() {
        var State_id = $('#state').val();
        var dist = $('#distict').val();
        if (dist == "") {
              $("#divdistributor").css("display", "none");
        }
    $.ajax({
       // url: '/Home/FillCity',
        url: '@Url.Action("FillDistict", "Home")',
        type: "GET",
        dataType: "JSON",

        data: { State: State_id },
        success: function (data) {
            var markup = "<option value=''> -- Select District --</option>";
            for (var x = 0; x < data.length; x++) {
                markup += "<option value=" + data[x].Value + ">" + data[x].Text + "</option>";
                //alert(markup)
            }
            $("#distict").html(markup).show();
        },
        error: function (reponse) {
            alert("error : " + reponse);
        }
    });
  }
</script>
<!--fill dealer according to district id-->
<script>
    function FillDealer() {
        var district_id = $('#distict').val();

        $.ajax({
             url: '@Url.Action("FillDealerList", "Home")',
            type: "GET",
            dataType: "JSON",

            data: { District: district_id },
            success: function (show) {
                $("#Distributorid").html("");
                var procemessage = "<option value=''>Select your Nearest & Known Distributor Point</option>";
                $("#Distributorid").html(procemessage).show();
                $("#divdistributor").css("display", "block");
                for (var x = 0; x < show.length; x++) {

                    $("#Distributorid").append($('<option></option>').val(show[x].Value).html(show[x].Text));
                }
                },
            error: function (reponse) {
                alert("error : " + reponse);
            }
        });
    }
</script>
<!--referral code javascropt-->
<script>
    function myFunction() {
        var checkBox = document.getElementById("referrral");
        if (checkBox.checked == true) {
            document.getElementById("referralcode").style.display = "block";

        } else {
            document.getElementById("referrral").style.display = "block";
            document.getElementById("referrral1").style.display = "block";
            document.getElementById("referralcode").style.display = "none";

        }
    }
</script>
<script>
    jQuery(function ($) {
        if ($.fn.owlCarousel && $(".vm-public-login .owl-carousel").length && !$(".vm-public-login .owl-carousel").hasClass("owl-loaded")) {
            $(".vm-public-login .owl-carousel").owlCarousel({
                autoplay: true,
                lazyLoad: false,
                loop: true,
                responsiveClass: true,
                autoHeight: false,
                autoplayTimeout: 3000,
                autoplayHoverPause: true,
                smartSpeed: 800,
                nav: false,
                dots: true,
                navText: ["<i class='fa fa-chevron-left'></i>", "<i class='fa fa-chevron-right'></i>"],
                responsive: {
                    0: { items: 1 },
                    600: { items: 1 },
                    1024: { items: 1 },
                    1366: { items: 1 }
                }
            });
        }
    });
</script>



<script>
    var mobile_loading = false;
    var email_loading = false;
    var mobile_clientId = null;
    var email_clientId = null;
    var mobile_verify = false;
    var email_verify = false;
    window.addEventListener('load', () => {
        var sendBtnOtp = document.getElementById('btnsendOTP');
        var txtmobileno = document.getElementById('txtmobileno');
        var resend_btn_otp = document.getElementById('resend_btn_otp');
        var otp_continer_inputs = document.getElementById('otp_continer_inputs');
        otp_continer_inputs.classList.add('hidden');
        

        $('input[name="Mobile"]').keyup(e => {
            $('#mobile_err').text('');
            if (mobile_loading) return;
            var mobileno = e.target.value;

            if (mobileno.length == 10) {
                mobile_loading = true;

                $('#Mobileno_spinner').show();

                fetch('@Url.Action("IsMobileEmailExist")?entry=' + mobileno + '&type=mobile')
                    .then(res => res.json())
                    .then(data => {
                        $('#Mobileno_spinner').hide();
                        mobile_loading = false;

                        if (data.status == true) {
                            $('#otp_continer_inputs').show();
                            mobile_clientId = data.client_id;
                            $('#codeBox1').focus();
                            $('input[name="Mobile"]').hide();
                        }
                        else if (data.status == false) {
                            mobile_clientId = null;
                            var message = data.message;
                            $('#mobile_err').text(message);
                            $('#otp_continer_inputs').hide();
                            $('#singup').focus();
                        }
                    })
                    .catch(err => {
                        mobile_loading = false;
                        mobile_clientId = null;
                        $('#Mobileno_spinner').hide();
                    })

            }
            else {
                $('#txtmobileno_spinner').hide();
            }

        })


        $('#btnsendOTP_Email').click(() => {
            $('#email_err').text('');
            if (email_loading) return;
            var emaild = $('input[type="email"]').val();

            if (!emaild) {
                return $('#email_err').text('Please Enter Email');
            }
            $('#btnsendOTP_Email').hide();

            email_loading = true;

            $('#Email_spinner').show();

            fetch('@Url.Action("IsMobileEmailExist")?entry=' + emaild + '&type=email')
                    .then(res => res.json())
                    .then(data => {
                        $('#Email_spinner').hide();
                        email_loading = false;

                        if (data.status == true) {
                            $('#otp_continer_inputs_email').show();
                            $('#btnsendOTP_Email').hide();
                            $('input[type="email"]').hide();
                            email_clientId = data.client_id;
                            $('#codeBox5').focus();
                        }
                        else if (data.status == false) {
                            email_clientId = null;
                            $('#singup').focus();
                            var message = data.message;
                            $('#email_err').text(message);
                            $('#btnsendOTP_Email').show();
                        }
                    })
                    .catch(err => {
                        email_loading = false;
                        mobile_clientId = null;
                        $('#Email_spinner').hide();
                    })


        })

    })
    function getCodeBoxElement(index) {
        return document.getElementById('codeBox' + index);
    }

    function isNumber(evt) {
        var iKeyCode = (evt.which) ? evt.which : evt.keyCode
        if (iKeyCode != 46 && iKeyCode > 31 && (iKeyCode < 48 || iKeyCode > 57))
            return false;
        return true;
    }

    function onKeyUpEvent(index, event) {
        $('#mobile_err').text('');
        const eventCode = event.which || event.keyCode;
        if (getCodeBoxElement(index).value.length === 1) {
            if (index !== 4) {
                getCodeBoxElement(index + 1).focus();
            } else {
                getCodeBoxElement(index).blur();
                // Submit code
                $('#Mobileno_spinner').show();
                var otp = $('#codeBox1').val() + $('#codeBox2').val() + $('#codeBox3').val() + $('#codeBox4').val();
                fetch('@Url.Action("OtpVerify")?type=mobile&clientId=' + mobile_clientId + '&otp=' + otp)
                    .then(res => res.json())
                    .then(data => {
                        $('#Mobileno_spinner').hide();
                        if (data.status) {
                            $('#otp_continer_inputs').hide();
                            $('input[name="Mobile"]').show();
                            document.querySelector('input[name="Mobile"]').setAttribute('disabled', 'disabled');
                            mobile_clientId = null;
                            $('#success_mobile_img').show();
                            document.querySelector('input[type="email"]').removeAttribute('disabled');
                            $('#btnsendOTP_Email').show();
                            mobile_verify = true;

                            fetch('@Url.Action("fetchEmail_Mobile_sts_Data")')
                                .then(res => res.json())
                                .then(data1 => {
                                    if (data1.type == 'OFF') {
                                        email_verify = true;
                                        $('#btnsendOTP_Email').hide()
                                    }
                                    else {
                                        $('#btnsendOTP_Email').show()
                                    }
                                    if (email_verify && mobile_verify) {
                                        block_inputs();
                                        document.getElementById('SignUp').removeAttribute('disabled');
                                    }

                                    $('#Mobile_Id').val(data.id);
                                })
                        }
                        else {
                            $('#mobile_err').text(data.message);
                        }
                    })
                    .catch(err => {
                        mobile_clientId = null;
                    })
            }
        }
        if (eventCode === 8 && index !== 1) {
            getCodeBoxElement(index - 1).focus();
        }
    }

    function onKeyUpEvent_Email(index, event) {
        $('#email_err').text('');
        const eventCode = event.which || event.keyCode;
        if (getCodeBoxElement(index).value.length === 1) {
            if (index !== 8) {
                getCodeBoxElement(index + 1).focus();
            } else {
                getCodeBoxElement(index).blur();
                // Submit code
                $('#Email_spinner').show();
                var otp = $('#codeBox5').val() + $('#codeBox6').val() + $('#codeBox7').val() + $('#codeBox8').val();
                fetch('@Url.Action("OtpVerify")?type=email&clientId=' + email_clientId + '&otp=' + otp)
                    .then(res => res.json())
                    .then(data => {
                        $('#Email_spinner').hide();
                        if (data.status) {
                            $('#otp_continer_inputs_email').hide();
                            $('input[type="email"]').show();
                            document.querySelector('input[type="email"]').setAttribute('disabled', 'disabled');
                            email_clientId = null;
                            $('#success_email_img').show();
                            email_verify = true;
                            if (email_verify && mobile_verify) {
                                block_inputs(true);
                                document.getElementById('SignUp').removeAttribute('disabled');
                            }
                            $('#Email_Id').val(data.id);
                        }
                        else {
                            $('#email_err').text(data.message);
                        }
                    })
                    .catch(err => {
                        mobile_clientId = null;
                    })
            }
        }
        if (eventCode === 8 && index !== 1) {
            getCodeBoxElement(index - 1).focus();
        }
    }

    function onFocusEvent(index) {
        for (item = 1; item < index; item++) {
            const currentElement = getCodeBoxElement(item);
            if (!currentElement.value) {
                currentElement.focus();
                break;
            }
        }
    }
</script>



<script>
    window.addEventListener('load', () => {
        var txtemail = document.getElementById('txtemail');
        var resend_btn_otp_email = document.getElementById('resend_btn_otp_email');

        $('#otp_continer_inputs_email').hide();
       
    })
    function getCodeBoxElement(index) {
        return document.getElementById('codeBox' + index);
    }
    
    function onFocusEvent(index) {
        for (item = 1; item < index; item++) {
            const currentElement = getCodeBoxElement(item);
            if (!currentElement.value) {
                currentElement.focus();
                break;
            }
        }
    }
</script>


<script>
    var inputs = document.getElementById('frmsignup').querySelectorAll('input[type="text"], input[type="password"], input[type="email"], select');
    var mobile_ref = document.getElementsByName('Mobile')[0];
    var email_ref = document.querySelector('input[type="email"]');

    $('#btnsendOTP_Email').hide();
    disable_inputs();

    




    function disable_inputs() {
        for (i = 0; i < inputs.length; i++) {
            if (!['Mobile'].includes(inputs[i].getAttribute('id'))) {
                inputs[i].setAttribute('disabled', true);
                inputs[i].value = '';
            }
        }
    }

    function block_inputs(email_verify = false) {
        var omit_field = ['Mobile'];
        if (email_verify) {
            omit_field.push('Email');
        }
        for (i = 0; i < inputs.length; i++) {
            if (!omit_field.includes(inputs[i].getAttribute('id'))) {
                inputs[i].removeAttribute('disabled');
                inputs[i].value = '';
            }
        }
    }




</script>



<script>
    $(document).ready(function () {
        $("#CaptchaInput").val("").prop("disabled", false);
        // 🔒 Signup fields initially disable
      
        $("#verifyCaptchaBtn").click(function () {
            var captchaInput = $("#CaptchaInput");

            if (captchaInput.length === 0) {
                console.error("❌ Captcha input field not found!");
                return;
            }

            var input = captchaInput.val().trim();
            if (input === "") {
                $("#captcha_msg").text("Please enter captcha first.")
                    .removeClass("text-success")
                    .addClass("text-danger");
                return;
            }

            // Spinner show + success image hide
            $("#captcha_spinner").removeClass("d-none");
            $("#captcha_success_img").addClass("d-none");
            $("#captcha_msg").text("");

            $.ajax({
                url: '@Url.Action("VerifyCaptcha", "Home")', // ✅ Correct controller & action
                type: 'POST',
                data: { inputCaptcha: input }, // ✅ Backend parameter match
                success: function (response) {
                    $("#captcha_spinner").addClass("d-none");

                    if (response.success) {
                        $("#captcha_msg").removeClass("text-danger")
                            .addClass("text-success")
                            .text(response.message);

                        $("#captcha_success_img").removeClass("d-none"); // ✅ Tick image show

                        $("#Mobile").prop("disabled", false);

                       
                    } else {
                        $("#captcha_msg").removeClass("text-success")
                            .addClass("text-danger")
                            .text(response.message);

                        $("#captcha_success_img").addClass("d-none");
                    }
                },
                error: function () {
                    $("#captcha_spinner").addClass("d-none");
                    $("#captcha_msg").removeClass("text-success")
                        .addClass("text-danger")
                        .text("Error verifying captcha.");
                }
            });
        });

        // 🔄 Captcha Refresh
        $("#refreshCaptcha").click(function () {
    
            $.get('@Url.Action("RefreshCaptcha", "Home")', function (data) {
                $("#captcha-text").text(data.Captcha); // New Captcha show
                $("#CaptchaInput").val("").prop("disabled", false);
                $("#verifyCaptchaBtn").prop("disabled", false);
                $("#captcha_msg").text("");
                $("#captcha_success_img").addClass("d-none");
            });
        });

    });


    </script>

